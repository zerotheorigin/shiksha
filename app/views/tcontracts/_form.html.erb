<%= form_with(model: tcontract, local: true) do |form| %>
  <% if tcontract.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tcontract.errors.count, "error") %> prohibited this tcontract from being saved:</h2>

      <ul>
        <% tcontract.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

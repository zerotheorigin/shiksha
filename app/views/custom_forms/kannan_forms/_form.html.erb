<%= form_with(model: custom_forms_kannan_form, local: true) do |form| %>
  <% if custom_forms_kannan_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_forms_kannan_form.errors.count, "error") %> prohibited this custom_forms_kannan_form from being saved:</h2>

      <ul>
        <% custom_forms_kannan_form.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question1 %>
    <%= form.text_area :question1 %>
  </div>

  <div class="field">
    <%= form.label :question2 %>
    <%= form.text_area :question2 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
